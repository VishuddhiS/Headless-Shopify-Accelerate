import * as path from 'path'

// eslint-disable-next-line
export default function contentful(moduleOptions) {
  const { contentful } = this.options
  const options = {
    ...contentful,
    ...moduleOptions,
  }
  this.addPlugin({
    src: path.resolve(__dirname, 'plugin.js'),
    options,
  })
}
